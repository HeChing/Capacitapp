specVersion: "v1"
serviceId: "capacitapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "capacitapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
